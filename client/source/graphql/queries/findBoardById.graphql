query FindBoardById($id: Int!) {
  board: findBoardById(id: $id) {
    id
    title
    description
    createdBy {
      id
      username
    }
    # TODO: Fix dataLoader
    # columns {
    #   ...ColumnFragment
    # }
    createdAt
    updatedAt
  }
}
