query FindBoardById($id: Int!) {
  board: findBoardById(id: $id) {
    id
    title
    description
    user {
      id
      username
    }
    # TODO: Fix dataLoader
    # columns {
    #   ...ColumnFragment
    # }
    createdAt
    updatedAt
  }
}
