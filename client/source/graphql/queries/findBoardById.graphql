query FindBoardById($id: Int!) {
  board: findBoardById(id: $id) {
    id
    title
    favorite
    createdBy {
      id
      username
    }
    columns {
      ...ColumnFragment
    }
    createdAt
    updatedAt
  }
}
