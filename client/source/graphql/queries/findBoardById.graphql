query FindBoardById($id: Int!) {
  board: findBoardById(id: $id) {
    id
    title
    createdBy {
      id
      username
    }
    columns {
      ...ColumnFragment
    }
    createdAt
    updatedAt
  }
}
