query FindBoardById($id: Int!) {
  board: findBoardById(id: $id) {
    id
    title
    description
    createdBy {
      id
      username
    }
    columns {
      ...ColumnFragment
    }
    createdAt
    updatedAt
  }
}
