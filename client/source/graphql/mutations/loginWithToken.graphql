mutation LoginWithToken($token: String!, $userInfo: UserInfo!) {
  loginWithToken(token: $token, userInfo: $userInfo) {
    errors {
      field
      message
    }
    user {
      ...UserFragment
    }
  }
}
